import React from 'react';
import PropTypes from 'prop-types';

const StatCard = (districtData) => {
  return(
    <div>
      <h2>District</h2>
      
    </div>
  )
}

StatCard.propTypes = {
  districtData: PropTypes.array.isRequired
}

export default StatCard;